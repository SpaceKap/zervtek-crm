# Database
# For Docker Compose, use the service name 'crm-db' as hostname
DATABASE_URL=postgresql://postgres:POSTGRES_PASSWORD@crm-db:5432/inquiry_pooler?schema=inquiry_pooler

# PostgreSQL Password (used by docker-compose.yml)
POSTGRES_PASSWORD=change-me-to-secure-password

# NextAuth
# For local dev use http://localhost:3000 and open the app at that exact URL (not 127.0.0.1) so session cookies match.
NEXTAUTH_URL=https://crm.zervtek.com
NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32

# OAuth Providers
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Optional: Default Wise Payment Link
DEFAULT_WISE_PAYMENT_LINK=https://wise.com/pay/business/ugoigd

# Optional: n8n Webhook Secret (for securing webhook endpoint)
N8N_WEBHOOK_SECRET=your-webhook-secret

# Optional: Redis (cache + rate limiting). With Docker Compose defaults to redis://redis:6379
# Omit for local dev without Redis (caching and rate limit then use in-memory/fallback)
# REDIS_URL=redis://redis:6379
